spring.config.import=optional:file:.env[.properties]
spring.application.name=backend
spring.datasource.url=jdbc:postgresql://aws-1-eu-north-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.yaejiwjjmhmicxrwychb
spring.datasource.password=${DB_PASSwORD}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=${PORT:8080}
jwt.secret=${JWT_SECRET}
jwt.expirationMs=86400000


spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.jpa.open-in-view=false

stripe.secretKey=${STRIPE_SECRET}
stripe.webhookSecret=${STRIPE_WEBHOOK_SECRET}
stripe.websiteUrl=https://sprintly-front.vercel.app/